/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com","source.unsplash.com", "picsum.photos"],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
